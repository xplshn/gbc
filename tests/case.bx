main() {
    extrn puts;
    x := 2;
    switch (x) {
        case 1:
            puts("one");
            break;
        case 2, 3:
            puts("two or three");
            break;
        default:
            puts("other");
    }
    return(0);
}
