// Tests enums
type enum Color {
    RED,
    GREEN,
    BLUE
};

main() {
    extrn puts;

    auto myColor = GREEN;
    if (myColor == GREEN) {
        puts("The color is green");
    }
    return(0);
}
